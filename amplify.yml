version: 1
backend:
    phases:
        build:
            commands:
                # - ldd --version
                # - nvm install 20
                - nvm use 20
                - npm ci
                - env | grep -e ADMIN_API_KEY -e BASE_URL >> .env
                - |
                    if [ "${AWS_BRANCH}" = "main" ] || [ "${AWS_BRANCH}" = "dev" ]; then 
                        npx amplify pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID 
                    else
                        npx amplify generate config  --branch dev --app-id $AWS_APP_ID 
                    fi

frontend:
    phases:
        preBuild:
            commands:
                - nvm use 20
                - npm ci
        build:
            commands:
                - npm run build
    artifacts:
        baseDirectory: .next
        files:
            - "**/*"
    cache:
        paths:
            - node_modules/**/*
