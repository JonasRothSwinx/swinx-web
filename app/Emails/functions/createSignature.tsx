import { ProjectManager } from "@/app/ServerFunctions/types";
import React from "react";

interface CreateSignatureParams {
    projectManager: ProjectManager;
}
export function createSignature({ projectManager }: CreateSignatureParams): React.JSX.Element {
    return <></>;
}
