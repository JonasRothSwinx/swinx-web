import { html } from "lit";

const htmlTemplate = html` <!DOCTYPE html>
    <html>
        <head>
            adbsajkd
        </head>
        <body>
            hi
        </body>
    </html>`;

const text = ``;

export default { htmlTemplate, text };
